<div class="container">
  <nav class="navbar navbar-default" role="navigation">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-navigation">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'todos' class="navbar-brand"}}Todos{{/link-to}}
    </div>

    <div class="collapse navbar-collapse" id="main-navigation">
      <ul class="nav navbar-nav">
        {{#link-to 'todos' tagName='li'}}{{#link-to 'todos'}}Home{{/link-to}}{{/link-to}}
        {{#link-to 'top_secret' tagName='li'}}{{#link-to 'top_secret'}}Top Secret{{/link-to}}{{/link-to}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
      {{#if isAuthenticated}}
        <li><a href="#" class="navbar-link">{{currentUser.email}}</a></li>
        <li><a href="#" class="navbar-link" {{action 'logout'}}><span class="glyphicon glyphicon-thumbs-down"></span> Log out</a></li>
      {{else}}
        <li>{{#link-to 'sessions.new'}}<span class="glyphicon glyphicon-thumbs-up"></span> Log in{{/link-to}}</li>
        <li>{{#link-to 'users.new'}}Register{{/link-to}}</li>
      {{/if}}
      </ul>
    </div>

  </nav>
  {{outlet}}
</div>
